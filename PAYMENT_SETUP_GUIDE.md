# Как добавить платежи в казино - Быстрый старт

## 1️⃣ Что я сделал

Я создал готовую систему платежей для казино, которая позволяет игрокам покупать монеты через:
- **Telegram Stars** ⭐ (встроенная валюта Telegram)
- **TON** 💎 (криптовалюта блокчейна)

## 2️⃣ Где находятся файлы

### Frontend компоненты:
- `frontend/src/components/Payment/PaymentModal.tsx` - Модальное окно платежей
- `frontend/src/components/Payment/PaymentModal.module.scss` - Стили модального окна
- `frontend/src/components/Payment/usePaymentStars.ts` - Hook для Telegram Stars
- `frontend/src/components/Payment/usePaymentTON.ts` - Hook для TON платежей
- `frontend/src/interface/Interface.tsx` - **ОБНОВЛЕНО** с кнопкой добавления монет

### Backend контролеры:
- `backend/src/controllers/PaymentController.ts` - **ОБНОВЛЕНО** с валидацией
- `backend/src/models/PaymentModel.ts` - Логика платежей (требует реализации)

### Документация:
- `TELEGRAM_INTEGRATION_GUIDE.md` - Полная документация по интеграции

## 3️⃣ Как это выглядит

```
┌─────────────────────────────┐
│  КАЗИНО ИНТЕРФЕЙС           │
├─────────────────────────────┤
│                      [100] 🪙 [+]  ← Кнопка добавления
│                                     (открывает PaymentModal)
│                                     
│           [SPIN]
│                                     
│          BET: 1                      
│                                     
│         WIN: 500                     
└─────────────────────────────┘

Нажал [+] ↓

┌──────────────────────────────┐
│ 💳 Add Coins                 │
├──────────────────────────────┤
│ Choose your payment method   │
│                              │
│ 100 coins    [⭐1]   [💎0.02]│
│ 500 coins    [⭐5]   [💎0.1] │
│ 1000 coins ★ [⭐10]  [💎0.2] │
│ 5000 coins   [⭐50]  [💎1]   │
└──────────────────────────────┘
```

## 4️⃣ Как использовать

### Для игроков:
1. Открывают казино в Telegram Mini App
2. Видят кнопку "+" рядом с балансом монет
3. Нажимают её → открывается меню платежей
4. Выбирают сумму и метод (Stars или TON)
5. Подтверждают платеж
6. Монеты добавляются к балансу
7. Могут играть в казино!

### Для разработчиков:

#### Импортировать компонент в свой интерфейс:
```tsx
import PaymentModal from '../components/Payment/PaymentModal';
import { useState } from 'react';

export const MyInterface = () => {
  const [showPaymentModal, setShowPaymentModal] = useState(false);

  return (
    <>
      <button onClick={() => setShowPaymentModal(true)}>
        Add Coins
      </button>
      
      <PaymentModal 
        isOpen={showPaymentModal}
        onClose={() => setShowPaymentModal(false)}
      />
    </>
  );
};
```

## 5️⃣ Что еще нужно сделать

### 🔴 TODO на Backend (важно!):

1. **Реализовать проверку баланса Telegram Stars:**
```typescript
// backend/src/models/PaymentModel.ts
async checkTelegramStarsBalance(userId: number): Promise<number> {
  // Подключить Telegram Bot API
  // Получить баланс звезд пользователя
}
```

2. **Реализовать проверку баланса TON:**
```typescript
// backend/src/models/PaymentModel.ts
async checkTONBalance(walletAddress: string): Promise<number> {
  // Подключить TON API (tonapi.io)
  // Получить баланс кошелька
}
```

3. **Реализовать проверку статуса платежа Telegram Stars:**
```typescript
async waitForTelegramStarsPayment(paymentId: number): Promise<boolean> {
  // Проверить статус в Telegram Bot API
}
```

4. **Реализовать проверку статуса платежа TON:**
```typescript
async waitForTONPayment(walletAddress: string, amount: number): Promise<boolean> {
  // Проверить транзакцию в TON Blockchain
}
```

### 🟡 TODO на Frontend (опционально):

1. Добавить анимацию при успешном платеже
2. Добавить историю платежей
3. Добавить receipts/квитанции
4. Добавить поддержку других валют

### 🔵 TODO на Инфраструктуре:

1. Получить Bot Token от @BotFather
2. Получить TON API ключ с tonapi.io
3. Настроить HTTPS для платежей
4. Добавить переменные окружения

## 6️⃣ Переменные окружения

Добавьте в `.env`:

```bash
# Telegram
TELEGRAM_BOT_TOKEN=ваш_bot_token

# TON
TONAPI_KEY=ваш_tonapi_ключ
VITE_WALLET_ADDRESS=ваш_кошелек_адрес

# Payment Config
VITE_PAYMENT_ENABLED=true
VITE_STARS_ENABLED=true
VITE_TON_ENABLED=true
```

## 7️⃣ Тестирование

### На локальной машине:
```bash
# 1. Установить зависимости
cd frontend
npm install
cd ../backend
npm install

# 2. Запустить Docker контейнеры
docker-compose up

# 3. Запустить backend
npm run dev

# 4. Запустить frontend (другой терминал)
npm run dev

# 5. Открыть http://localhost:5173
```

### На Telegram:
1. Создать Mini App через @BotFather
2. Установить URL вашего приложения
3. Открыть через Telegram (мобильное приложение)
4. Проверить что платежи работают

## 8️⃣ Структура платежного потока

```
Фронтенд                          Backend                    API
─────────────────────────────────────────────────────────────────

Пользователь нажал "+"
     ↓
PaymentModal открыт
     ↓
Выбрал метод & сумму
     ↓
handleStarsPayment() / handleTonPayment()
     ↓
window.Telegram.WebApp.invokeCustomMethod()    →  Telegram Server
                      или
TonConnect.sendTransaction()                    →  TON Blockchain
     ↓ (получил hash)
POST /api/payments
     ├── Валидация данных              ✓
     ├── Проверка баланса               → Telegram API / TON API
     ├── Создание записи платежа        → Database
     ├── Проверка статуса платежа       → Telegram API / TON API
     └── Добавление монет пользователю  → Database
     ↓
Response { success: true, coins: 1000 }
     ↓
updateCoins(1000)
     ↓
Казино: баланс обновлен, можно играть!
```

## 9️⃣ Безопасность

⚠️ **Важно:**

1. **Никогда не доверяй фронтенду!**
   - Всегда валидируй на бэкенде
   - Проверяй статус платежа в официальных API

2. **Защити Bot Token и ключи**
   - Используй только переменные окружения
   - Никогда не коммитьте в git

3. **Используй HTTPS**
   - Все платежи должны быть зашифрованы

4. **Rate limiting**
   - Ограничь количество платежных запросов с одного IP

5. **Логирование**
   - Логируй все платежи для аудита

## 🔟 Полезные ссылки

- [Telegram Bot API - Payments](https://core.telegram.org/bots/payments)
- [Telegram Mini Apps Documentation](https://core.telegram.org/bots/webapps)
- [TonConnect SDK](https://github.com/ton-connect/sdk)
- [TON API Documentation](https://tonapi.io)
- [TON Blockchain Explorer](https://tonscan.org)

---

## ✅ Чеклист для запуска в продакшене

- [ ] Получить Bot Token от @BotFather
- [ ] Получить TON API ключ
- [ ] Реализовать функции checkBalance и waitForPayment
- [ ] Настроить HTTPS
- [ ] Добавить логирование платежей
- [ ] Протестировать на Testnet (TON)
- [ ] Протестировать с реальными платежами
- [ ] Добавить обработку ошибок платежей
- [ ] Настроить мониторинг платежей
- [ ] Задокументировать для команды

---

**Версия:** 1.0.0  
**Дата:** 13 февраля 2026  
**Статус:** Готово для интеграции
